const util = require('node:util');

let message = '%s is learning MERN in batch having strength of %d';

// let welcome = util.format(message,'Anirudha',30);

// console.log(welcome);
console.log(util.format(message,'Anirudha',30));